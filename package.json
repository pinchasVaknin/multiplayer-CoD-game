{
  "name": "operator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OPERATOR - browser arena FPS. Milestone 1: core loop and movement.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
